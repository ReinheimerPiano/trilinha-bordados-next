FROM node:20-alpine

WORKDIR /app

# Dependências em camada separada para aproveitar cache do Docker
COPY package.json package-lock.json ./
RUN npm ci

# Os fontes vêm via bind mount do docker-compose
EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--hostname", "0.0.0.0", "--port", "3000"]
